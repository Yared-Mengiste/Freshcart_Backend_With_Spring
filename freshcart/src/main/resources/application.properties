spring.application.name=freshcart

# Database Configuration
# Ensure DATASOURCE_URL in Render starts with jdbc:postgresql://
spring.datasource.url=jdbc:postgresql://ep-gentle-sunset-ahr4y3h1-pooler.c-3.us-east-1.aws.neon.tech/freshcart_db?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_W3lMm4QABytO
spring.datasource.driver-class-name=org.postgresql.Driver

# Frontend Integration
#frontend.url=${FRONTEND_URL}

# JPA / Hibernate
spring.jpa.open-in-view=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server Port - Render will inject ${PORT}, fallback to 8080
server.port=${PORT:8080}

# Security
spring.security.user.name=admin
spring.security.user.password=admin

# Logging (Fixed comment syntax)
# logging.level.org.springframework=DEBUG
logging.level.root=INFO
logging.level.org.springframework=INFO

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nathnaelmesfin6284@gmail.com
# WARNING: Move this password to a Render Environment Variable!
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000